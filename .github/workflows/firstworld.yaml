name: MyFirstworkflow

on: 
  push:
    branches:
      - main

jobs:
  first-run:
    runs-on: ubuntu-latest

    steps: 
      - name: checkout reporitory
        uses: actions/checkout@v4

      - name: say Hello
        run:  | 
          echo "hello world" 
          echo "this is my forst workflow"
